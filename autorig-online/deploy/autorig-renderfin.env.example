# Renderfin service environment (install as /etc/autorig-renderfin.env)

# Data root: render artifacts, sqlite db, worker registry
RENDERFIN_DATA_DIR=/var/autorig/renderfin

# Public base for output_url construction (served by nginx alias /renderfin/render/)
RENDERFIN_PUBLIC_BASE_URL=https://autorig.online/renderfin

# Plain-HTTP ComfyUI worker hosts the adapter may talk to (anti-SSRF allowlist)
RENDERFIN_ALLOWED_HTTP_HOSTS=5.129.157.224,37.192.2.126,127.0.0.1,localhost

# Basic auth "user:password" for HTTPS worker edges (worker-4090.renderfin.qwertystock.com)
#RENDERFIN_WORKER_BASIC_AUTH=user:password

# Hunyuan3D image-to-3D via converter workers (preferred over ComfyUI when token set).
# Enabled+installed as of 2026-07: F2, F7, F13. Token lives on the workers as
# %LOCALAPPDATA%\AutoRig\hunyuan_api_token — use the same value here.
#HUNYUAN_API_TOKEN=...
#RENDERFIN_HUNYUAN_WORKERS=https://converter-f2.freestock.online,https://converter-f7.freestock.online,https://converter-f13.freestock.online
#RENDERFIN_HUNYUAN_QUALITY=standard

# Shared-GPU broker. Enable only after every managed node has the protected
# Comfy control listener and host bootstrap heartbeat deployed. Keep scoped
# broker credentials and per-node mTLS material in /srv/autorig/secrets, never
# in this example or process arguments.
#RENDERFIN_WORKLOAD_BROKER_ENABLED=1
#RENDERFIN_WORKLOAD_BROKER_URL=http://127.0.0.1:8000/api/workload-broker
#RENDERFIN_WORKLOAD_BROKER_TOKEN=<same value as AUTORIG_WORKLOAD_BROKER_RENDERFIN_TOKEN>
# Central FIFO claimants replay the same request while waiting.  A claimant
# which disappears is retired attempt-neutrally so it cannot block the farm.
#AUTORIG_WORKLOAD_WAITER_TTL=90
# Managed Comfy map. Every entry pins the live machine_* identity and canonical
# role. Central lifecycle/status/artifact traffic uses a way-fr loopback tunnel
# to the node's SYSTEM arbiter mTLS listener on 5200; plaintext 5199 and the
# converter listener are never fallback transports. `comfy_ai` nodes such as
# F5 require no converter URL because identity is read directly from mTLS
# `/status`. Full nodes additionally retain their authenticated converter
# status URL/token. CA, client certificate, private key and central bearer are
# protected files (CA/cert 0444 or stricter; key/token 0400/0440).
#RENDERFIN_GPU_CONTROL_NODES_JSON={"f5":{"capability_mode":"comfy_ai","physical_resource_id_string":"machine_...","workload_role":"ai_vision_primary","arbiter_control_url_string":"https://127.0.0.1:15205","central_token_file_string":"/srv/autorig/secrets/gpu-control/f5.token","central_tls_ca_file_string":"/srv/autorig/secrets/gpu-control/f5-ca.pem","central_tls_client_cert_file_string":"/srv/autorig/secrets/gpu-control/f5-client.pem","central_tls_client_key_file_string":"/srv/autorig/secrets/gpu-control/f5-client.key"}}

# Queue tuning (defaults shown)
#RENDERFIN_TASK_TIMEOUT_SECONDS=3600
#RENDERFIN_MANAGED_COMFY_NO_PROGRESS_TIMEOUT_SECONDS=3600
#RENDERFIN_PUMP_TICK_SECONDS=1.5
#RENDERFIN_DISPATCH_INTERVAL_SECONDS=5

# Turntable renderer (node + headless chrome + ffmpeg on this host)
#RENDERFIN_TURNTABLE_NODE=node
#RENDERFIN_TURNTABLE_SCRIPT=/root/autorig-online/tools/renderfin/glb_turntable.mjs
#RENDERFIN_TURNTABLE_CHROME=/usr/bin/chromium
#RENDERFIN_TURNTABLE_FFMPEG=ffmpeg
#RENDERFIN_TURNTABLE_SECONDS=6
