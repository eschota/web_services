--- a/bonsai_adapter.py
+++ b/bonsai_adapter.py
@@ BonsaiAdapter.status
             "loaded_model": self.loaded_model_id(),
+            "system_prompt_supported": True,
+            "system_prompt_models": ["bonsai2-27b"],
+            "unlimited_output_supported": True,
             "base_url": self.config.base_url,
@@ each model status
                     "context_tokens": int(entry.context_tokens),
+                    "system_prompt_supported": entry.id == "bonsai2-27b",
+                    "unlimited_output_supported": True,
@@ validate_max_tokens
+    if value == -1:
+        return -1
     if value < 1:
-        raise BonsaiRequestError("max_output_tokens must be positive")
+        raise BonsaiRequestError("max_output_tokens must be positive or -1 for unlimited")
@@ request body
+        requested_max_tokens = (
+            self.config.max_output_tokens if max_output_tokens is None
+            else int(max_output_tokens)
+        )
         body = {
-            "max_tokens": int(max_output_tokens or self.config.max_output_tokens),
+            "max_tokens": requested_max_tokens,
@@ response usage
-        usage = payload.get("usage")
-        return answer, reasoning, usage if isinstance(usage, dict) else {}
+        usage = dict(payload.get("usage") or {})
+        usage["requested_max_tokens"] = requested_max_tokens
+        return answer, reasoning, usage
@@ BonsaiAdapter.run_completion
         prompt: str,
         *,
+        system_prompt: str = "",
         model_id: Optional[str] = None,
@@ request body
-        body = {
-            "messages": [{"role": "user", "content": content}],
+        messages = []
+        clean_system_prompt = str(system_prompt or "").strip()
+        if clean_system_prompt:
+            messages.append({"role": "system", "content": clean_system_prompt})
+        messages.append({"role": "user", "content": content})
+        body = {
+            "messages": messages,

--- a/webserver_converter_glb.py
+++ b/webserver_converter_glb.py
@@ imports
     MAX_IMAGE_BYTES as BONSAI_MAX_IMAGE_BYTES,
+    MAX_PROMPT_CHARS as BONSAI_MAX_PROMPT_CHARS,
@@ AiVisionTask
     prompt: str
+    system_prompt: str = ""
@@ AiVisionTask.to_dict
         payload = asdict(self)
+        payload.pop("system_prompt", None)
@@ _submit_ai_task validation
         prompt = validate_prompt(raw_payload.get("prompt"))
+        system_prompt = str(raw_payload.get("system_prompt") or "").strip()
+        if len(system_prompt) + len(prompt) > BONSAI_MAX_PROMPT_CHARS:
+            raise BonsaiRequestError(
+                f"system_prompt and prompt exceed {BONSAI_MAX_PROMPT_CHARS} characters"
+            )
@@ task construction
         prompt=prompt,
+        system_prompt=system_prompt,
@@ process_ai_vision_task
         task.prompt,
+        system_prompt=task.system_prompt,
