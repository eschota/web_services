# 🚀 Коммиты с сервера

## ✅ Статус: ВСЕ РАБОТАЕТ!

Сервер полностью настроен для коммитов в GitHub репозиторий.

### 🔧 Настройки
- **Автор**: Константин Ермолаев
- **Email**: eschota@gmail.com
- **Remote**: git@github.com:eschota/web_services.git
- **SSH**: ✅ Настроен и работает

## 🛠️ Способы коммитов

### Вариант 1: Простой скрипт (рекомендуется)
```bash
./commit.sh "feat: add new awesome feature"
```

### Вариант 2: Ручной способ
```bash
git add .
git commit -m "feat: add new feature"
git push origin main
```

### Вариант 3: Селективный коммит
```bash
git add specific-file.js
git commit -m "fix: bug in specific file"
git push origin main
```

## 📝 Правила хороших коммитов

### Типы коммитов:
- `feat:` - новая функциональность
- `fix:` - исправление багов
- `docs:` - изменения в документации
- `style:` - форматирование кода
- `refactor:` - рефакторинг
- `test:` - тесты
- `chore:` - обслуживание

### Примеры:
```bash
./commit.sh "feat: add user authentication system"
./commit.sh "fix: resolve memory leak in worker"
./commit.sh "docs: update API documentation"
./commit.sh "refactor: simplify database queries"
```

## 🔍 Проверка статуса

```bash
# Проверить статус репозитория
git status

# Посмотреть последние коммиты
git log --oneline -5

# Проверить remote
git remote -v
```

## ⚠️ Важно

- **Не коммитить секреты** (пароли, API ключи)
- **Не коммитить большие файлы** (>50MB)
- **Всегда проверять** `git status` перед коммитом
- **Писать понятные сообщения** на русском или английском

## 🎯 Быстрые команды

```bash
# Отменить последний коммит (если не запушен)
git reset --soft HEAD~1

# Отменить изменения в файле
git checkout -- filename

# Создать новую ветку
git checkout -b feature/new-feature

# Вернуться на main
git checkout main
git pull origin main
```

---

**Готово к работе!** 🎉
