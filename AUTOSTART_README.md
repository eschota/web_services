# 🚀 Автозапуск AutoRig Online

## ✅ Статус: НАСТРОЕН И РАБОТАЕТ

Проект AutoRig Online автоматически запускается при перезагрузке сервера.

## 📋 Текущая конфигурация

### SystemD сервис: `autorig.service`
- **Статус**: ✅ Активен и включен для автозапуска
- **Путь к проекту**: `/opt/autorig-online/`
- **Виртуальное окружение**: `/opt/autorig-online/venv/`
- **Рабочая директория**: `/opt/autorig-online/backend/`
- **Порт**: `127.0.0.1:8000`

### Автозапуск
- ✅ Сервис включен в автозагрузку
- ✅ Автоматический перезапуск при сбое
- ✅ Логирование в journald

## 🛠️ Управление сервисом

### Основные команды
```bash
# Статус сервиса
sudo systemctl status autorig

# Остановить сервис
sudo systemctl stop autorig

# Запустить сервис
sudo systemctl start autorig

# Перезапустить сервис
sudo systemctl restart autorig

# Отключить автозапуск
sudo systemctl disable autorig

# Включить автозапуск
sudo systemctl enable autorig
```

### Просмотр логов
```bash
# Последние 50 строк логов
sudo journalctl -u autorig -n 50

# Следить за логами в реальном времени
sudo journalctl -u autorig -f

# Логи за последний час
sudo journalctl -u autorig --since "1 hour ago"
```

## 🔧 Настройка и обслуживание

### Переустановка сервиса
Если нужно переустановить сервис:
```bash
./setup_autostart.sh
```

### Резервное копирование
```bash
# База данных
cp /opt/autorig-online/backend/db/autorig.db /opt/autorig-online/backup/

# Конфигурация
cp /opt/autorig-online/backend/.env /opt/autorig-online/backup/
```

### Мониторинг ресурсов
```bash
# Использование памяти сервисом
ps aux | grep uvicorn

# Мониторинг портов
netstat -tlnp | grep :8000

# Проверка доступности
curl http://127.0.0.1:8000/
```

## 🚨 Устранение неисправностей

### Сервис не запускается
1. Проверить логи: `sudo journalctl -u autorig -n 20`
2. Проверить виртуальное окружение: `ls -la /opt/autorig-online/venv/bin/`
3. Проверить зависимости: `cd /opt/autorig-online/backend && ../venv/bin/pip list`

### Приложение недоступно
1. Проверить порт: `netstat -tlnp | grep :8000`
2. Проверить логи приложения: `sudo journalctl -u autorig -f`
3. Проверить Nginx: `sudo systemctl status nginx`

### Высокое потребление ресурсов
1. Проверить количество подключений воркеров
2. Очистить старые файлы: `find /var/autorig/uploads -type f -mmin +1440 -delete`
3. Перезапустить сервис: `sudo systemctl restart autorig`

## 📊 Метрики и здоровье

### Проверка здоровья
```bash
# HTTP статус
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/

# JSON ответ API
curl http://127.0.0.1:8000/api/health

# Статус воркеров
curl http://127.0.0.1:8000/api/workers/status
```

### Автоматические проверки
Добавьте в crontab для автоматического мониторинга:
```bash
# Каждые 5 минут проверять статус сервиса
*/5 * * * * systemctl is-active autorig || echo "AutoRig service is down" | mail -s "Service Alert" admin@example.com
```

## 🔄 Обновление проекта

### Без остановки сервиса
```bash
# Копировать новые файлы
sudo cp -r /root/autorig-online/* /opt/autorig-online/

# Обновить зависимости
cd /opt/autorig-online/backend
../venv/bin/pip install -r requirements.txt --upgrade

# Перезапустить сервис
sudo systemctl restart autorig
```

### С остановкой сервиса
```bash
# Остановить сервис
sudo systemctl stop autorig

# Обновить код
sudo cp -r /root/autorig-online/* /opt/autorig-online/

# Запустить сервис
sudo systemctl start autorig
```

---

## 🎯 Резюме

✅ **Сервис настроен и работает**
✅ **Автозапуск при перезагрузке включен**
✅ **Автоматический перезапуск при сбое**
✅ **Полное логирование**
✅ **Мониторинг и обслуживание настроены**

Сервер готов к работе 24/7! 🚀
